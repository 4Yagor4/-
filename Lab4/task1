№7 Сформировать одномерный массив из значений максимальных элементов столбцов матрицы А размером 3 × 5.

s = []
#Введите столбец матрицы из трех значений
for i in range(5):
    a = [int(x) for x in input().split()]
    s.append(max(a))
print(*s)

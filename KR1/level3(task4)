#Задан массив В. Найти максимальный элемент в массиве и заменить его суммой элементов стоящих до него.

B = [3,2,5,6,7,9,2]
maxel = max(B)
for i in range(len(B)):
    if B[i] == maxel:
        sum = sum(B[:i])
        B[i] = sum
print(B)
